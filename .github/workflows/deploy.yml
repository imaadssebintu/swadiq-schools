name: Deploy to Server

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v3
    
    - name: Deploy to server
      uses: appleboy/ssh-action@v0.1.5
      with:
        host: ${{ secrets.HOST }}
        username: ${{ secrets.USERNAME }}
        password: ${{ secrets.PASSWORD }}
        command_timeout: 30m
        script: |
          cd ~/docker
          # Stop and remove existing container
          docker stop swadiq-app || true
          docker rm swadiq-app || true
          # Clean up old files
          rm -rf swadiq-schools
          # Clone latest code
          git clone https://github.com/ImaadDean/swadiq-schools.git
          cd swadiq-schools
          # Build new image with increased timeout and no cache conflicts
          DOCKER_BUILDKIT=1 docker build --no-cache -t swadiq-schools .
          # Run container with restart policy
          docker run -d --name swadiq-app -p 8080:8080 --restart unless-stopped swadiq-schools